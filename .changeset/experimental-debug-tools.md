---
"@houwert/conductor": minor
---

Add experimental React Native tooling: `debug` (Hermes/Fusebox debugger — evaluate JS, component tree, element inspection), `network` (HTTP traffic logs and requests), and `profile` (CPU, memory, and React commit profiling).
