---
"@houwert/conductor": minor
---

Add `pinch`, `rotate-gesture`, and `gesture` commands for two-finger and arbitrary multi-touch gestures, backed by a new multi-finger gesture-path route in the iOS and Android drivers.
