{
  "name": "conductor-studio",
  "version": "0.1.0",
  "private": true,
  "description": "Conductor Studio — Maestro test workbench, agentic test writing, and test case management",
  "author": "Houwert",
  "license": "UNLICENSED",
  "type": "module",
  "main": "dist-electron/main.js",
  "scripts": {
    "dev": "concurrently -k -n vite,electron \"vite\" \"pnpm build:electron && electron .\"",
    "dev:vite": "vite",
    "build:electron": "node build-electron.mjs",
    "build": "tsc --noEmit && pnpm typecheck:electron && vite build && pnpm build:electron",
    "typecheck:app": "tsc --noEmit",
    "typecheck:electron": "tsc -p tsconfig.main.json --noEmit",
    "dist": "pnpm prepare-conductor && pnpm build && electron-builder --config electron-builder.yml",
    "dist:dev": "pnpm prepare-conductor && pnpm build && electron-builder --config electron-builder.yml",
    "dist:release": "pnpm prepare-conductor && pnpm build && electron-builder --config electron-builder.release.cjs --publish always",
    "preview": "vite preview",
    "prepare-conductor": "tsx scripts/prepare-conductor.ts",
    "postinstall": "tsx scripts/prepare-conductor.ts",
    "build:icon": "tsx scripts/generate-icon.ts"
  },
  "dependencies": {
    "@codemirror/autocomplete": "^6.18.0",
    "@codemirror/lint": "^6.8.0",
    "@conductor/studio-ui": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "chokidar": "^4.0.0",
    "electron-updater": "^6.3.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "ws": "^8.21.0",
    "yaml": "^2.8.3",
    "zod": "^4.3.6",
    "zustand": "^5.0.0"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "@types/node": "^22.0.0",
    "@types/react": "^19.1.8",
    "@types/react-dom": "^19.1.6",
    "@types/ws": "^8.18.1",
    "@vitejs/plugin-react": "^4.6.0",
    "concurrently": "^9.0.0",
    "electron": "^34.0.0",
    "electron-builder": "^26.0.0",
    "esbuild": "0.27.4",
    "tsx": "^4.19.0",
    "typescript": "~5.8.3",
    "vite": "^7.0.4"
  }
}
